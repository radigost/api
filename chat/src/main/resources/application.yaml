persistence:
  messages:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: mypass

server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

servicediscovery:
  api:
    url: localhost
    port: 8080
  counter:
    url: localhost
    port: 8082

opentracing:
  jaeger:
    service-name: OpenTracingChatApp
    enabled: true
    udp-sender:
      host: localhost
